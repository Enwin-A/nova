# Secrets — never commit
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
pytest-cache-files-*/

# Pip-installed packages live outside the repo (site-packages).
# Below: generated/local artifacts produced by running ingest or the app.

# User-supplied corpus (not in repo — download separately)
data/reviews.jsonl

# Generated by ingest (SQLite + vector index)
data/reviews.db
data/chroma/
data/chroma_*/
data/_embeddings_cache.json
data/test_reviews.db

# Generated by ingest
evals/eval_ground_truth.json

# Runtime agent logs
traces/*.jsonl

# IDE / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
